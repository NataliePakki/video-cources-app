import { FormatDuractionPipe } from './format-duraction.pipe';

describe('FormatDuractionPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatDuractionPipe();
    expect(pipe).toBeTruthy();
  });
});
